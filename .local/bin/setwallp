#!/bin/sh
# This is my wallpaper setter script. Require package "xwallpaper" in order to work. Also, remember to type full path to the wallpaper, else the wallpaper restore script won't work
[ -f $1 ] && xwallpaper --zoom $1 && echo "$1" > ~/.config/.lastwallpdir || xwallpaper --zoom $(< ~/.config/.lastwallpdir)
